# Конфигурация окружения для systemd-сервиса telegram-channels-to-md
# Скопируйте этот файл в systemd/telegram-channels-to-md.env и отредактируйте значения.
# Юнит читает этот файл через EnvironmentFile= …

# Включить подробный лог скрипта (set -x)
DEBUG=0

# Настройка автора/коммитера через переменные окружения (без глобального git config)
GIT_AUTHOR_NAME=channels-bot
GIT_AUTHOR_EMAIL=bot@example.invalid
GIT_COMMITTER_NAME=channels-bot
GIT_COMMITTER_EMAIL=bot@example.invalid

# К какому удалённому и ветке пушить (если не задан GIT_PUSH_URL)
GIT_REMOTE=origin
GIT_BRANCH=main

# Вариант A (рекомендуется под systemd): HTTPS с токеном (Personal Access Token)
# Пример: токен с правами на push, встроенный в URL (не требует SSH-ключей)
# Уберите # и подставьте свой токен и репозиторий:
# GIT_PUSH_URL=https://<YOUR_GITHUB_TOKEN>@github.com/hightemp/python_telegram_channels_to_md.git

# Вариант B: SSH ключ
# Укажите путь к приватному ключу, которым можно пушить в репозиторий:
# SSH_KEY_PATH=/home/hightemp/.ssh/id_ed25519
# При необходимости можно переопределить полностью команду SSH:
# GIT_SSH_COMMAND=ssh -i /home/hightemp/.ssh/id_ed25519 -o StrictHostKeyChecking=accept-new

# Автосоздание Git-репозитория при его отсутствии (обычно НЕ нужно, оставьте 0)
GIT_AUTO_INIT=0
GIT_INIT_DEFAULT_BRANCH=main